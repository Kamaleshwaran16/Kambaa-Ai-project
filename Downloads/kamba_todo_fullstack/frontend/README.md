# Frontend - Kamba TODO (React + Vite)

Run:
1. `npm install`
2. `npm run dev` (default port 5173)

This app calls the backend at http://localhost:8000 by default. Change `VITE_API_BASE` in `.env` for deployment.
